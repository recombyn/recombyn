# 示例品牌规范（社区示例 · 文件 skill）

把本目录复制为 `data/design_skills/<your_key>/`，含 `_meta.json` + `SKILL.md`（可选 logo）即可注册。

## 规则（保持简单）
- 主色 `#0B1F33`，强调色 `#E8A838`
- 圆角偏小（4～8）
- 文案短句、克制

## 输出
- UI 壳用矢量；需要摄影氛围再生图
