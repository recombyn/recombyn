# Agent-generated plaza docs (gitignored media URLs may expire)

Produced by `npm run plaza:agent-showcase` — each `*.json` has a hydrated
`document` with real `image` / `video` / `lottie` srcs. Seed loads these only
when media is present (no rectangle mock boards).
