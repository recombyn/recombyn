# Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID (Web)
# Authorized JavaScript origins: http://localhost:3000 (and prod URL)
# Authorized redirect URIs: http://localhost:3000/login/google/callback
GOOGLE_CLIENT_ID=
# Optional alias
# VITE_GOOGLE_CLIENT_ID=

# Docs site (help + legal). Unset in local → http://localhost:5175 (apps/docs `pnpm dev`).
# Production builds default to https://docs.recombyn.com
# VITE_DOCS_URL=http://localhost:5175
# VITE_DOCS_URL=https://docs.recombyn.com

