# `@resume-scene/web`

React editor + home/agent UI (Vite). Engineering docs:

- [Canvas architecture](../../docs/canvas-architecture.md) — RCB, SVG hosts, Path2D hit-test, pencil ribbon, LOD
- [Web data layer](../../docs/web-frontend.md) — oRPC + TanStack Query + nuqs + home→editor boot
- [Scene JSON](../../docs/scene-json-spec.md)

```bash
# from repo root
npm run dev:web    # http://localhost:3000
npm test --workspace=apps/web
```
