import type { ReactNode } from 'react';
import type { SceneDocument } from '@/components/rcb/sceneNode';

/**
 * Placeholder — copy `src/private.example/mockup` → `src/private/mockup`
 * and replace with the closed-source implementation (internal distribution only).
 */
export default function MockupSessionHost(_props: {
  document: SceneDocument;
}): ReactNode {
  return null;
}
