# Start local Langfuse via WSL Docker (not Docker Desktop).
# Preferred: .\start-for-windows.ps1
Set-StrictMode -Version Latest
$ErrorActionPreference = "Stop"
& "$PSScriptRoot\start-for-windows.ps1"
